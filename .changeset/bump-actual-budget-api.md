---
"callsheet": patch
---

Upgrade `@actual-app/api` to 26.5.2 so the budget connector stays in sync with newer Actual Budget server migrations (older client versions refuse to sync against an upgraded server).
